import 'package:flutter/material.dart';

class MailBox extends StatelessWidget {
  const MailBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey,
      child: Center(child: Text("MailBox......")),
    );
  }
}
